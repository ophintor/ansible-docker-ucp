---
- hosts: docker
  gather_facts: false
  become_user: root
  become: true

  vars_files:
    - ../vars.yml

  tasks:
    - name: Enable and start docker service
      systemd:
        name: docker
        enabled: yes
        state: started
